extends layout

block content
    h1= title

    ul
        for book in book_list
            li
                a(href=book.url) #{book.title}
                |   (#{book.author.name})
        else
            li Could not find any books.